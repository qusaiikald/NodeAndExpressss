/*
        path obj model

// i talked from the doc and i didint use init becouse it is already in the node 
const path = require('path');

// some methodes

// Base file name
console.log(path.basename(__filename));//your gotta get the basename with out the whole path

// File Extension
console.log(path.extname(__filename));

// Creat path opj
console.log(path.parse(__filename));
console.log(path.parse(__filename).base);

// concatenate paths
// ../test/hello.html
console.log(path.join(__dirname, "test", "hello.html"));
*/

